{
  "name": "doonooch",
  "version": "0.1.0",
  "devDependencies": {
    "parcel": "^1.12.4",
    "prettier": "^1.19.1",
    "pug": "^3.0.1",
    "sass": "^1.26.1"
  },
  "scripts": {
    "serve": "parcel serve src/**/*",
    "build": "parcel build src/**/*",
    "deploy": "yarn build && firebase deploy"
  }
}
